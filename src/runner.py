# runner script copied from ex0

import calculate_chi_square
import json

if __name__ == '__main__':
    
    """
    myjob1 = Fun() # one for preproc and one for calculating?
    with myjob1.make_runner() as runner:
        runner.run()
        
        for key, value in myjob1.parse_output(runner.cat_outut()):           
            print(key, value, "\n", end='')
    
    """
    
